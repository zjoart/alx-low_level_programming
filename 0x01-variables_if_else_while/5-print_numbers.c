#include <stdio.h>
/**
 * main - Print single digit numbers of base 10 frm 0
 * Return: Always 0 (Successful)
 */
int main(void)
{
	int g;

	for (g = 0; g < 10; g++)
	{
		printf("%d", g);
	}
	printf("\n");
	return (0);
}
